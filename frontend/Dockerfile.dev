# Étape 1 : Construction de l'application Angular
FROM node:20-alpine AS build

WORKDIR /app

# Copier les fichiers de dépendances
COPY package*.json ./

# Installer les dépendances (y compris les devDependencies pour le build)
RUN npm ci

# Copier le reste du code source
COPY . .

# Construire l'application avec la configuration docker
RUN npx ng build --configuration docker

# Étape 2 : Serveur Nginx pour servir les fichiers statiques
FROM nginx:alpine

# Copier la configuration Nginx personnalisée pour le dev
COPY nginx.dev.conf /etc/nginx/conf.d/default.conf

# Copier les fichiers construits depuis l'étape précédente
# Assurez-vous que le chemin correspond à "outputPath" dans angular.json (ici dist/frontend)
COPY --from=build /app/dist/frontend /usr/share/nginx/html

# Exposer le port 80
EXPOSE 80

# Démarrer Nginx
CMD ["nginx", "-g", "daemon off;"]
